# Motion video references (not committed)

Three `.mov` motion references ship with the handoff package but are excluded from git (163MB total). They live in the original zip:

`~/Downloads/DROP_Claude_Handoff_Package_v1.2_With_Mock_Content.zip` → `DROP_Claude_Handoff_Package/02-motion/`

| File | What it specifies |
|---|---|
| `lusion-card-motion-reference.mov` | Menu-card stack, fan, arrival, stagger, and flip choreography (Scene 7.3) |
| `pixel-transition-reference.mov` | Scroll-linked, reversible pixel replacement between scenes (Scenes 7.5 / 7.7) |
| `art-pieces-motion-reference.mov` | Art Pieces editorial rhythm, reveal, masking, and parallax (Scene 7.9) |

Before implementing those scenes, inspect the clip (the master brief, Section 0.1, requires it). If the zip is gone, the behavioral references are also live sites: lusion.co, runrobrun.com, x2ycreative.com — behavior only, never copy assets or code.
