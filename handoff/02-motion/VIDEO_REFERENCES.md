# Motion video references

The three `.mov` motion references are committed alongside this file (158MB total — they make clones heavy, but the repo is fully self-contained).

| File | What it specifies |
|---|---|
| `lusion-card-motion-reference.mov` (58MB) | Menu-card stack, fan, arrival, stagger, and flip choreography (Scene 7.3) |
| `pixel-transition-reference.mov` (34MB) | Scroll-linked, reversible pixel replacement between scenes (Scenes 7.5 / 7.7) |
| `art-pieces-motion-reference.mov` (66MB) | Art Pieces editorial rhythm, reveal, masking, and parallax (Scene 7.9) |

Before implementing those scenes, inspect the corresponding clip — the master brief (Section 0.1) requires it. The live behavioral references are lusion.co, runrobrun.com, and x2ycreative.com — behavior only, never copy assets or code.
